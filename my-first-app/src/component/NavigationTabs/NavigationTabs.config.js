import {
    NavigationTabsMap as SourceNavigationTabsMap,
} from 'SourceComponent/NavigationTabs/NavigationTabs.config';

// ? TODO: Since override is to JavaScript, you need to handle overridden ENUMS
export const NavigationTabsMap = { ...SourceNavigationTabsMap };
